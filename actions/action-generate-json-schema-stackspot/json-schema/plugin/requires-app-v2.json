{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "connections": {
      "$ref": "classpath:json-schema/commons/requires-connections.json"
    },
    "plugins": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^([a-z0-9]+(?:-[a-z0-9]+)*)\\/([a-z0-9]+(?:-[a-z0-9]+)*)$"
      }
    },
    "actions": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^([a-z0-9]+(?:-[a-z0-9]+)*)\\/([a-z0-9]+(?:-[a-z0-9]+)*)$"
      }
    }
  }
}