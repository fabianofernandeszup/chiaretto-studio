{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "plugins": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^([a-z0-9]+(?:-[a-z0-9]+)*)\\/([a-z0-9]+(?:-[a-z0-9]+)*)$"
      }
    },
    "actions": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^([a-z0-9]+(?:-[a-z0-9]+)*)\\/([a-z0-9]+(?:-[a-z0-9]+)*)$"
      }
    },
    "variables": {
      "$ref": "classpath:json-schema/commons/requires-variables.json"
    }
  }
}